{
  "name": "claude-io-sidecar",
  "version": "0.0.1",
  "private": true,
  "description": "claude-io audio/STT/TTS sidecar process. Spawned by the VSCode extension host, owns the microphone and speaker, runs STT (whisper.cpp) and TTS (Piper) inference, and communicates with the extension via JSON-RPC over stdin/stdout.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./dist/main.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.5.0"
  }
}
